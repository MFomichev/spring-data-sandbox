spring:
  datasource:
    url: jdbc:tc:postgresql:12.3:///databasename
  jpa:
    properties:
      hibernate.format_sql: true
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
